<?xml version="1.0" encoding="UTF-8" ?>
<doc>
<title>if statement</title>

<para>
<ptitle>if statement</ptitle>
　The if statement is a syntax that determines whether or not to execute a part of the script based on a conditional decision, such as "if XX do YY".<r/>
　The syntax is as follows:<r/>
<bq>
if(expression)<r/>
	Statement or block<r/>
else<r/>
	Statement or block<r/>
</bq>
　The first "statement or block" is executed when the result of evaluating expression is true, and the second "statement or block" is executed when the result of evaluation is false. After <kw>else</kw>, you do not need to write it if you do not need it.<r/>
<example>
	if(a==b)<r/>
		inform("a and b are the same");<r/>
<r/>
	if(a&lt;b)<r/>
	{<r/>
		var t;<r/>
		t=a; a=b; b=t; // swaps a and b <r/>
	}<r/>
<r/>
	if(a==b)<r/>
		inform("a and b are the same");<r/>
	else<r/>
		inform("a and b are different");<r/>
</example>
</para>

<para>
<ptitle>Correspondence between if and else</ptitle>
　else has the rule "corresponds to the previous, if which does not yet correspond to an else".<r/>
<r/>
For example,<r/>
<bq>
	if(expr) // ★<r/>
		if(expr) // ●<r/>
			statement;<r/>
		else // ●<r/>
			statement;<r/>
	else // ★<r/>
		statement;<r/>
</bq>
　The else of ★ corresponds to the if of ★, and the else of ● corresponds to the if of ●.<r/>
　Freestyle languages such as TJS2 even incorrectly indent
<bq>
	if(expr) // ★<r/>
		if(expr) // ●<r/>
			statement;<r/>
	else // ●<r/>
		statement;<r/>
</bq>
　Even if you write, you need to be careful because the correspondence is as above.<r/>
　If you want a clear break,<r/>
<bq>
	if(expr) { // ★<r/>
		if(expr) // ●<r/>
			statement;<r/>
	}<r/>
	else // ★<r/>
		statement;<r/>
</bq>
　please take the method of enclosing in a block like the above.<r/>
</para>
</doc>
